using Godot;
public class BaseBlock : Area2D
{
	public void OnAreaEntered(Area2D area)
	{
		// if (area is Ball ball)
		// {
		// 	ball.direction = (ball.direction + new Vector2(0, _bounceDirection)).Normalized();
		// }
	}
}
